# ═══════════════════════════════════════════════════════════════════════════════
# aws2lakehouse — Python Dependencies
# ═══════════════════════════════════════════════════════════════════════════════
# Canonical dependencies are in pyproject.toml. This file is for convenience
# when installing outside of pip install -e .
#
# Minimal:  pip install pyyaml requests
# Full:     pip install -r requirements.txt
# Dev:      pip install -e ".[dev]"
# ═══════════════════════════════════════════════════════════════════════════════

# ── Core (always required) ─────────────────────────────────────────────────────
pyyaml>=6.0                 # YAML spec parsing (factory, pipeline configs)
requests>=2.28              # HTTP client for Foundation Model API (--ai mode)

# ── AWS Scanning (optional — for live inventory from AWS accounts) ─────────────
# boto3>=1.28               # pip install aws2lakehouse[aws]

# ── Databricks (optional — for workspace deployment & AI features) ─────────────
# databricks-sdk>=0.20      # pip install aws2lakehouse[databricks]

# ── Development & Testing ──────────────────────────────────────────────────────
# pytest>=7.0               # pip install aws2lakehouse[dev]
# pytest-cov>=4.0
# ruff>=0.1
