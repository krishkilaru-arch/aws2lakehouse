resources:
  jobs:
    vendor_data_ingestion:
      name: "[prod] finance/vendor_data_ingestion"
      description: "Migrated from AWS: auto_loader → finance_bronze"
      tasks:
        - task_key: vendor_data_ingestion
          notebook_task:
            notebook_path: pipelines/finance/vendor_data_ingestion
            base_parameters:
              CATALOG: "acme_prod"
              SCHEMA: "finance_bronze"
              TABLE: "vendor_data_ingestion"
          max_retries: 2
          timeout_seconds: 3600
      tags:
        domain: "finance"
        layer: "bronze"
        classification: "internal"
