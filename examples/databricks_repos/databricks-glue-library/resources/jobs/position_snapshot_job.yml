resources:
  jobs:
    position_snapshot_job:
      name: "[prod] finance/position_snapshot_job"
      description: "Migrated from AWS: jdbc → finance_bronze"
      tasks:
        - task_key: position_snapshot_job
          notebook_task:
            notebook_path: pipelines/finance/position_snapshot_job
            base_parameters:
              CATALOG: "acme_prod"
              SCHEMA: "finance_bronze"
              TABLE: "position_snapshot_job"
          max_retries: 2
          timeout_seconds: 3600
      tags:
        domain: "finance"
        layer: "bronze"
        classification: "internal"
