resources:
  jobs:
    silver_transformation:
      name: "[prod] finance/silver_transformation"
      description: "Migrated from AWS: auto_loader → finance_bronze"
      tasks:
        - task_key: silver_transformation
          notebook_task:
            notebook_path: pipelines/finance/silver_transformation
            base_parameters:
              CATALOG: "acme_prod"
              SCHEMA: "finance_bronze"
              TABLE: "silver_transformation"
          max_retries: 2
          timeout_seconds: 3600
      tags:
        domain: "finance"
        layer: "bronze"
        classification: "confidential"
